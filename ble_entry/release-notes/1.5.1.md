Optional GitHub updates, including a fix to keep update prompts available after Android recreates the app screen.

Choose Update, Later or Skip this version. APKs are checked against the expected package, newer version, checksum and existing signing certificate. Android controls installation, and enrollment/calibration stay saved.

Includes all background BLE and minimizable proximity cards. Real door operation and TOTP delivery remain disabled. Internet access is used only for GitHub app updates.

Phones on 1.4 or older need to install this APK once without uninstalling Door. Phones on 1.5 can use App updates → Check for updates now.
